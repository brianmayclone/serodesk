<UserControl x:Class="SeroDesk.Views.Widgets.WidgetContainer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="150" d:DesignWidth="200">
    
    <Grid x:Name="MainGrid">
        <!-- Widget Content -->
        <ContentPresenter x:Name="WidgetContent"/>
        
        <!-- Drag Handle (invisible overlay) -->
        <Rectangle x:Name="DragHandle"
                  Fill="Transparent"
                  Cursor="SizeAll"
                  Visibility="Collapsed"/>
                  
        <!-- Context Menu for Widget Options -->
        <Grid.ContextMenu>
            <ContextMenu>
                <MenuItem Header="Lock Position" 
                         IsCheckable="True"
                         IsChecked="{Binding IsLocked}"
                         Click="LockWidget_Click"/>
                <Separator/>
                <MenuItem Header="Remove Widget" 
                         Click="RemoveWidget_Click"/>
            </ContextMenu>
        </Grid.ContextMenu>
    </Grid>
</UserControl>